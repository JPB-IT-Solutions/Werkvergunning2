<%= form_for(@enquirymeasures) do |f| %>
    <% if @enquirymeasures.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@enquiry.errors.count, "error") %> prohibited this enquiry from being saved:</h2>

          <ul>
            <% @enquirymeasures.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div>
      <%= f.label :Referentie %><br>
      <%= f.text_field :reference %> <br>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
