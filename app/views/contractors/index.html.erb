<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "Werkvergunningssysteem", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Home", root_path %></li>

        <li>
          <% if can? :update, @enquiry %>
              <%= link_to 'Gebruikers', users_path %></li>
          <% end %>

        <li>
          <% if can? :update, @enquiry %>

            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Beheer <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Gemachtigden", representatives_path %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Log out", destroy_user_session_path, method: "delete" %>
                </li>
              </ul>
        <% end %>

          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", edit_user_registration_path %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", destroy_user_session_path, method: "delete" %>
              </li>
            </ul>
          </li>

          </ul>
    </nav>
  </div>
</header>
<br><br><br><br>

<p id="notice"><%= notice %></p>

<div id="main-container" class="container-fluid">
  <div class="row">
    <div id="sidebar" class="col-xs-2">
      <!-- sidebar content -->
      <% if can? :manage, @enquiry %>
          <%= link_to 'Nieuwe aannemer', new_contractor_path, class: 'btn btn-success' %>
      <% end %>
    </div>

    <div id="content" class="col-xs-10">
      <p> boodschap</p>

<h1>Goedgekeurde aannemers</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Naam</th>
      <th>Email</th>

    </tr>
  </thead>

  <tbody>
    <% @contractors.each do |contractor| %>
      <tr>
        <td><%= contractor.name %></td>
        <td><%= contractor.email %></td>

        <td><%= link_to 'Show', contractor, class: 'btn btn-primary btn-xs' %></td>
        <td><%= link_to 'Edit', edit_contractor_path(contractor), class: 'btn btn-info btn-xs'  %></td>
        <td><%= link_to 'Destroy', contractor, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-xs' %></td>
      </tr>
    <% end %>
  </tbody>
</table>
    </div>
  </div>
</div>
<br>


<%= link_to 'New Contractor', new_contractor_path %>
