<%= form_for(@representative) do |f| %>
    <% if @representative.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@representative.errors.count, "error") %> prohibited this representative from being
            saved:</h2>

          <ul>
            <% @representative.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    </div>

    <div class="field">
      <%= f.label :Naam %><br>
      <%= f.text_field :name %>
    </div>


    <div class="field">
      <%= f.label :Telefoon %><br>
      <%= f.number_field :telephone %>
    </div>


    <div class="field">
      <%= f.label :email %><br>
      <%= f.email_field :email %>
    </div>

    <div class="field">
      <%= f.label :handtekening %><br>
      <%= f.email_field :signature %>
    </div>


    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
